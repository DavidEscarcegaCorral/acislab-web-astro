---
import ButtonGroup from '../ui/ButtonGroup.astro';
---

<header
    id="site-header"
    class="fixed top-0 left-0 z-50 w-full bg-white transition-all duration-300">
    <div class="mx-auto flex max-w-7xl items-center justify-center px-6 py-4">
        <a
            id="header-logo"
            href="/"
            class="text-brand-secondary pointer-events-none mr-0 w-0 overflow-hidden text-xl font-bold tracking-widest whitespace-nowrap uppercase opacity-0 transition-all duration-500 ease-in-out">
            ACISLAB
        </a>

        <ButtonGroup />
    </div>
</header>

<script>
    const logo = document.getElementById('header-logo');
    const heroTitle = document.getElementById('hero-title');

    if (logo && heroTitle) {
        const observer = new IntersectionObserver(
            ([entry]) => {
                if (!entry.isIntersecting) {
                    logo.classList.remove('w-0', 'opacity-0', 'mr-0', 'pointer-events-none');
                    logo.classList.add('w-[130px]', 'opacity-100', 'mr-8');
                } else {
                    logo.classList.add('w-0', 'opacity-0', 'mr-0', 'pointer-events-none');
                    logo.classList.remove('w-[130px]', 'opacity-100', 'mr-8');
                }
            },
            {
                threshold: 0,
                rootMargin: '-80px 0px 0px 0px',
            },
        );

        observer.observe(heroTitle);
    }
</script>
